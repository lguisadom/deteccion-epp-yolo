# Dependencias del proyecto — Pregunta 3 (Detección de cumplimiento de EPP con YOLO)
# Instalar con:  pip install -r requirements.txt
#
# NOTA sobre GPU/CUDA:
#   En Linux, "pip install ultralytics" ya instala torch con soporte CUDA automáticamente.
#   Verificar con:  python -c "import torch; print(torch.cuda.is_available())"  -> debe dar True
#
# Versiones fijadas para reproducibilidad (entorno verificado: Python 3.12, RTX 4070, CUDA 13).

ultralytics==8.4.56
torch==2.12.0
torchvision==0.27.0
opencv-python==4.13.0.92
numpy==2.4.6
matplotlib==3.10.9
PyYAML==6.0.3

# Para descargar el dataset desde Roboflow (Fase 1):
roboflow

# Para leer claves desde el archivo .env:
python-dotenv
